[project]
name = "example-pyproject"
version = "0.0.1a1"
license = { text = "MIT" }
authors = [
  { name = "Venkatesh Kolluru", email = "vk0046@uah.edu" }
]
maintainers = [
  { name = "john", email = "jim.john@alaska.edu" }
]
requires-python = ">=3.10"
dependencies = [
  "numpy >= 1.25"
]

docs = ["mkdocs-material"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
